Do not use of remove these files.

They are not completed and are intended to create a reusable IBAN libraries that can be overloaded for multiple countries using the Diamond model but where the storage of the routing is done in a separate contract.